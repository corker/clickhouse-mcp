version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gosec

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
