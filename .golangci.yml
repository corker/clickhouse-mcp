version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gosec
  settings:
    revive:
      rules:
        # Package-level doc comments are kept; per-symbol doc comments on
        # exported identifiers are not required — names carry the meaning.
        - name: package-comments
        - name: exported
          disabled: true

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
